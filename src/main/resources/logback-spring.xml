<configuration>
    <springProperty scope="context" name="env" source="app.firebase.logging.environment"/>
    <springProperty scope="context" name="minLevel" source="app.firebase.logging.min-level"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger - %msg%n</pattern>
        </encoder>
    </appender>
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>

    <!-- Ide jön a custom Firebase appendered -->
    <appender name="FIREBASE" class="com.project.sls.logging.FirebaseLogAppender">
        <collection>sls_logs</collection>
        <environment>${env}</environment>
    </appender>
</configuration>